"""
    1. Check if the question exists (the question is the same as one of the saved one's)
    2. Add news questions to the saved file with all the questions (CSV)
    3. Creates the CSV if that doesn't exists

        FORMAT LOADED > CSV: ID(Mod.+NumQ);Module;Question;C_Answer,W_Answer##W_Answer2##W_Answer3;

"""